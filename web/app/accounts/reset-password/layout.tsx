import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Reset Password - Nexio",
};

export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return children;
}
